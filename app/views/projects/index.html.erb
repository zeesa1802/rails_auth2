<p style="color: green"><%= notice %></p>




        <header class="py-5">
            <div class="container px-lg-5">
                <div class="p-4 p-lg-5 bg-light rounded-3 text-center">
                <h1 class="display-5 fw-bold">Projects</h1>
                    <div class="m-4 m-lg-5">
                        
                        <% if (current_user.role == 'project_manager' || current_user.role == 'admin')%>
                        <a class="btn btn-primary btn-lg" href="<%= new_project_path %>">Create New</a>
                         <% end %>
                    </div>
                </div>
            </div>
        </header>
  <div class="container px-4 px-lg-5">

 
          
            <!-- Content Row-->
            <div class="row gx-4 gx-lg-5">
            <% @projects.each do |project| %>
          
                <div class="col-md-4 mb-5">
                    <div class="card h-100">
                        <div class="card-body">
                            <h2 class="card-title"><%= project.name.capitalize %></h2>
                            <p class="card-text">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quod tenetur ex natus at dolorem enim! Nesciunt pariatur voluptatem sunt quam eaque, vel, non in id dolore voluptates quos eligendi labore.</p>
                        </div>
                        <div class="card-footer"><a class="btn btn-primary btn-sm" href="<%= project_path(project) %>">More Info</a></div>
                    </div>
                </div>

                
            <% end %>


            </div>
        </div>
